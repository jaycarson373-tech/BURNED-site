# topblast-robinhood

This project was generated by The Mean Machine 0.1.0. Read PROJECT.md and mean-machine.json first. Use the included modules before recreating integrations.

Run npm run verify after changing shared behavior. The source integrity check detects deviations from the exported kit; an intentional edit needs a documented rationale in PROJECT.md and updated evidence, not a silently reset fingerprint. Never copy credentials, live contracts, or branding from the source project. Runtime configuration must come from this project's environment.

Unit tests, contract tests, fork tests, and live transactions are distinct evidence. Do not call this deployed, audited, or production-ready based only on compilation. Consult each selected module's requirements in PROJECT.md.

Keep PROJECT.md current with the product request, decisions, commands, evidence, and concrete remaining work so a fresh chat can resume without reading old conversations.
